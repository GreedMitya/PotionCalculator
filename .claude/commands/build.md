Run a full Gradle build for the PotionCalculator project and report the results.

Steps:
1. Run `./gradlew build` with the `--console=plain` flag for readable output
2. If the build fails, analyze the error output and identify:
   - Which module/file caused the failure
   - The specific error message
   - A suggested fix
3. If the build succeeds, report:
   - Build time
   - Any warnings that should be addressed
   - Confirmation that all tasks completed

Use `--warning-mode=all` to surface deprecation warnings.
